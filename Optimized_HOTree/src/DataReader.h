#include "define.h"

std::vector<DataRecord> readDataFromDataset(const std::string& filename, int N);
std::vector<DataRecord> readDataFromDataset(const std::string& filename);
std::vector<std::string> LoadDictionary(const std::string& filename);